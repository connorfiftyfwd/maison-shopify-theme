{% schema %}
{
  "name": "Navigation",
  "blocks": [
    {
      "type": "section",
      "name": "Navigation",
      "settings": [
        {
          "id": "title",
          "type": "text",
          "label": "Title"
        }
      ]
    }
  ]
}
{% endschema %}

<ul class="side-nav__navi js-sideNav">
  {% for block in section.blocks %}
    {%- assign hash = block.settings.title | downcase | handleize -%}
    {% render 'utility-nav' hash: hash, title: block.settings.title %}
  {% endfor %}
</ul>
